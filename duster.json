{
    "include": [
        "app",
        "config",
        "database",
        "routes",
        "tests"
    ],
    "exclude": [
    ],
    "scripts": {
        "lint": {
            "phpstan": ["./vendor/bin/phpstan", "analyse", "--xdebug"]
        },
        "fix": {
            "phpstan": ["./vendor/bin/phpstan", "analyse", "--xdebug"]
        }
    },
    "processTimeout": 360
}